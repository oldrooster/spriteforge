## Things I'd like to build / features to add

### Standalone AI Generate
Launch AI Image generate as a stand alone tool (Maybe next to + New Asset) have a "Generate" button

### Crop Region.
* When Selecting the are to crop with the cursor (draws yellow rectangle) have the Crop region input boxes dynamically update
* if updating the crop region input boxes, dynamically update the yellow rectangle crop Region 
* Remove the crop image button. It's implied Instead
  * Download button crops and downloads
  * Save button - crops and saves image back to library. 
  * Move Download and Save buttons next to each other horizontally.
* Under aspect ration, add an "All" button that selects the entire image, removing the Select Entire Image button by itself (UI Improvement)
* Allow zooming on the image for more fine grained cropping

### Resize Images
* Once an image or view has been selected, remove the image upload box and have the thumbnails along the top with the original and result pictures vertically stacked.
* Allow a single image or multiple (view selection)
* When only a single image is selected, the download is a PNG, when multiple, a ZIP
* Remove Resize images and instead:
  * Download button resizes and downloads
  * Save button resizes and saves back to library
  * Move Download and Save buttons next to each other horizontally.

### Make Transparent
* Make minimum zoom out less than 50% so that bigger images can fit without the vertical scroll bars
* Move zoom buttons (+ - Fit) under neath the image to the buttom right (opposite the preview background colour selector)
* Move Download and Save buttons next to each other horizontally.

### Prompt Library
 * Add this under settings in the top right
 * Allow to Manage prompts (Add/Edit/Delete) 
 * Specify asset category for each prompt (e.g. Character, Background, Sound, object, UI)
 * Add selection for image gen or video gen (or both)
 * on the AI Geberator and AI Video generator.
   *  Remove the list of prompts and replace with a dropdown box to select (based on the generation category (video/image)). 
   * Ensure dropdown allows filtering based on typing (contains filter)
   * When selecting a prompt ask whether to replace or append to current prompt (unless its empty then replace)
   * Make the text prompt windows larger so its easier to see.
   * Include a Save Prompt button
  

## Bugs
* On first load no assets show. I need to create an asset, then hit back and they appear. 

